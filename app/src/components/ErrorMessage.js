import React from 'react';

const ErrorMessage = () => {
  return (
    <div>
      <p className="message--api-error">
        Could't connect with the API. Try again later
      </p>
    </div>
  );
};

export default ErrorMessage;
